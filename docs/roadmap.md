# Roadmap (lightweight)
- Admin: "Refresh dietaries" button
- Optional: Newbook webhook sync for live updates
- Printable kitchen view / PDF export
- Monitoring: minimal health endpoint + latency log (no PII)
